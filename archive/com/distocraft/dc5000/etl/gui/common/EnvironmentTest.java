/**
 *  commenting out these test cases to generate coverage as compilation of this test case if failing
 */
 
 /*
package com.distocraft.dc5000.etl.gui.common;

import org.junit.Test;
import static org.hamcrest.CoreMatchers.*;
import static org.junit.Assert.*;

import com.distocraft.dc5000.etl.gui.common.Environment.Type;

*/

/**
 * @author eheijun
 *
 */

public class EnvironmentTest {
    /*
  @Test
  public void testStats() {
    Environment env = new EnvironmentStats();
    assertThat(env.getType(), is(Type.STATS));
    assertThat(env.getDwhName(), is("dwh"));
    assertThat(env.showLoadings(), notNullValue());
    assertThat(env.showAggregations(), notNullValue());
    assertThat(env.showOldReAggregations(), notNullValue());
    assertThat(env.showNewReAggregations(), notNullValue());
    assertThat(env.showDatasourceLog(), notNullValue());
    assertThat(env.showBusyhourInfo(), notNullValue());
    assertThat(env.showMonitoringRules(), notNullValue());
    assertThat(env.showUnmatchedTopology(), notNullValue());
    assertThat(env.showAdminConfiguration(), notNullValue());
    assertThat(env.showEBSUpgrader(), notNullValue());
    assertThat(env.showNodeVersionUpgrader(), notNullValue());
    assertThat(env.showUserAdministration(), notNullValue());
    assertThat(env.showManual(), notNullValue());
  }

  @Test
  public void testEvents() {
    Environment env = new EnvironmentEvents();
    assertThat(env.getType(), is(Type.EVENTS));
    assertThat(env.getDwhName(), is("dwh_coor"));
    assertThat(env.showLoadings(), notNullValue());
    assertThat(env.showAggregations(), notNullValue());
    assertThat(env.showOldReAggregations(), notNullValue());
    assertThat(env.showNewReAggregations(), notNullValue());
    assertThat(env.showDatasourceLog(), notNullValue());
    assertThat(env.showBusyhourInfo(), notNullValue());
    assertThat(env.showMonitoringRules(), notNullValue());
    assertThat(env.showUnmatchedTopology(), notNullValue());
    assertThat(env.showAdminConfiguration(), notNullValue());
    assertThat(env.showEBSUpgrader(), notNullValue());
    assertThat(env.showNodeVersionUpgrader(), notNullValue());
    assertThat(env.showUserAdministration(), notNullValue());
    assertThat(env.showManual(), notNullValue());
  }

  @Test
  public void testMixed() {
    Environment env = new EnvironmentMixed();
    assertThat(env.getType(), is(Type.MIXED));
    assertThat(env.getDwhName(), not(""));
    assertThat(env.showLoadings(), notNullValue());
    assertThat(env.showAggregations(), notNullValue());
    assertThat(env.showOldReAggregations(), notNullValue());
    assertThat(env.showNewReAggregations(), notNullValue());
    assertThat(env.showDatasourceLog(), notNullValue());
    assertThat(env.showBusyhourInfo(), notNullValue());
    assertThat(env.showMonitoringRules(), notNullValue());
    assertThat(env.showUnmatchedTopology(), notNullValue());
    assertThat(env.showAdminConfiguration(), notNullValue());
    assertThat(env.showEBSUpgrader(), notNullValue());
    assertThat(env.showNodeVersionUpgrader(), notNullValue());
    assertThat(env.showUserAdministration(), notNullValue());
    assertThat(env.showManual(), notNullValue());
  }

  @Test
  public void testNone() {
    Environment env = new EnvironmentNone();
    assertThat(env.getType(), is(Type.NONE));
    assertThat(env.getDwhName(), nullValue());
    assertThat(env.showLoadings(), nullValue());
    assertThat(env.showAggregations(), nullValue());
    assertThat(env.showOldReAggregations(), nullValue());
    assertThat(env.showNewReAggregations(), nullValue());
    assertThat(env.showDatasourceLog(), nullValue());
    assertThat(env.showBusyhourInfo(), nullValue());
    assertThat(env.showMonitoringRules(), nullValue());
    assertThat(env.showUnmatchedTopology(), nullValue());
    assertThat(env.showAdminConfiguration(), nullValue());
    assertThat(env.showEBSUpgrader(), nullValue());
    assertThat(env.showNodeVersionUpgrader(), nullValue());
    assertThat(env.showUserAdministration(), nullValue());
    assertThat(env.showManual(), nullValue());
  }
*/
}
